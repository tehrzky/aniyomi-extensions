ext {
    extName = 'DramaCool'
    extClass = '.DramaCool'
    extVersionCode = 1
}

apply from: "$rootDir/common.gradle"

android {
    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}
